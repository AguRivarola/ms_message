version: '3.8'
services:
    nginx:
        image: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
        - "80:80" 
    hostname: nginx
    networks:
        msg-net:
            aliases:
            - nginx
networks:
  msg-net:
    external: true